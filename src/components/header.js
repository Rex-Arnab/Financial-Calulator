import React from 'react'

export default function BasicHeader({title}) {
    return (
        <header className="bg-purple-600 text-white p-2 text-center font-bold rounded-t-xl">{ title }</header>
    )
}
